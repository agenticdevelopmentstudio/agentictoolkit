---
id: c8438764-89c9-4286-9b58-10c9cd175a97
title: AIModelCatalog
domain: agentictoolkit://cookbook/ai-plugin-kit/ai-model-catalog
type: ingredient
version: 1.0.2
status: review
language: en
created: '2026-09-23'
modified: '2026-09-25'
author: Mike Fullerton
copyright: 2026 Mike Fullerton
license: MIT
summary: 'AIPluginKit''s shared model table: canonical model facts, per-gateway pricing/context
  terms, and curated overrides merged into one lookup.'
platforms:
- swift
- macos
tags:
- ai
- model-catalog
- provider
- engine
depends-on: []
related: []
references:
- packages/apple/AgenticToolkit/AIPluginKit/AIModelCatalog.swift (agentictoolkit)
- packages/apple/AgenticToolkit/AIPluginKit/AIPluginDescriptor.swift (agentictoolkit)
- packages/apple/AgenticToolkit/AIPluginKit/AIPluginManager.swift (agentictoolkit)
- packages/apple/AgenticToolkit/AIPluginKit/ModelCapability.swift (agentictoolkit)
- packages/apple/AgenticToolkit/AIPluginKit/model-catalog.json (agentictoolkit)
- packages/apple/AgenticToolkit/Tests/AIPluginKitTests/AIModelCatalogTests.swift (agentictoolkit)
- packages/apple/AIPlugins/tools/generate_model_catalog.py (agentictoolkit)
approved-by: ''
approved-date: ''
---

# AIModelCatalog

## Overview

`AIModelCatalog` is the shared model table `AIPluginKit` ships as a bundled
resource (`model-catalog.json`): one `Model` entry per canonical model id,
independent of which gateway serves it, plus a per-`(templateId, spelling)`
`Offering` table for the terms (context window, output limit, token prices)
that genuinely differ between gateways. It is a **logic** component — no
visual surface — used by every `AIPluginDescriptor.ProviderTemplate` to answer
"what do we know about this model, and what does this gateway charge for it."
`resolve(model:template:)` merges the shared `Model` facts, the matching
`Offering`, and a template's curated `ModelDetail` into one `ResolvedModel`
for display. The catalog is generated by
`AIPlugins/tools/generate_model_catalog.py` from adh's public catalog and
committed as `AIPluginKit/model-catalog.json`; `AIModelCatalog.shared` loads it
once, falling back to an empty (never nil) catalog if the resource is missing
or unreadable.

## Behavioral Requirements

- **model-identity**: A `Model` MUST be uniquely identified by its canonical
  `id`; its `aliases` array MUST list every additional spelling a gateway uses
  for that same model (e.g. `meta-llama/Llama-3.3-70B-Instruct`,
  `@cf/meta/llama-3.3-70b-instruct-fp8-fast`).
- **model-decoding-defaults**: Decoding a `Model` MUST default `aliases` and
  `capabilities` to empty arrays and `description` to `nil` when those keys
  are absent from the JSON payload, rather than failing to decode the entry.
- **offering-is-per-gateway-spelling**: An `Offering` MUST be looked up under
  the exact model-name spelling that one gateway (`templateID`) uses; the same
  model's terms at a different gateway, or under its canonical id, MUST NOT be
  returned.
- **offering-fields-independently-optional**: Each field of `Offering`
  (`contextWindow`, `maxOutput`, `inputCostPerM`, `outputCostPerM`) MUST be
  independently representable as absent (`nil`) rather than defaulted to
  zero, because "unknown" and "zero" are different facts about a gateway's
  terms.
- **lookup-resolution-order**: `model(named:)` MUST resolve a name by
  exact-case match against `byName` first, THEN by lowercased match against
  `byName`, THEN by canonicalizing the name and matching `byCanonicalID`; it
  MUST return `nil` when none of the three match.
- **canonicalization-strips-namespace**: `canonicalID(_:)` MUST trim leading
  and trailing whitespace/newlines, lowercase the result, and drop any path
  segment up to and including the last `/`.
- **canonicalization-strips-free-suffix**: `canonicalID(_:)` MUST strip a
  trailing `:free` suffix (an OpenRouter price tier, not a different model).
- **canonicalization-strips-quantization-suffix**: `canonicalID(_:)` MUST
  strip at most one trailing suffix from the fixed set `-fp8-fast`, `-fp8`,
  `-fp4`, `-bf16`, `-awq`, `-int8`, `-int4`, testing them in that order so
  `-fp8-fast` matches before the shorter `-fp8` can.
- **canonicalization-preserves-other-suffixes**: `canonicalID(_:)` MUST NOT
  strip a suffix outside the quantization set — `-turbo`, `-latest`, `-fast`,
  `-thinking` MUST survive unchanged, because they name a genuinely different
  model or alias, not a precision variant.
- **resolve-merges-three-sources**: `resolve(model:template:)` MUST merge, for
  one `(model name, template)` pair, the shared `Model` facts (via
  `model(named:)`), the `Offering` for `template.id` under the given spelling
  (via `offering(templateID:model:)`), and the template's curated
  `ModelDetail` (via `template.modelDetail(for:)`, an exact-id match) into one
  `ResolvedModel`.
- **curated-description-wins**: `resolve(model:template:)` MUST use the
  curated `ModelDetail.description` when it is present and non-empty, and
  MUST fall back to the shared `Model.description` when the curated value is
  absent or an empty string.
- **curated-tools-wins**: `resolve(model:template:)` MUST use the curated
  `ModelDetail.tools` value when present; when absent, it MUST derive `tools`
  from whether the shared `capabilities` array contains `"tools"`, yielding
  `nil` (unknown), not `false`, when `capabilities` is empty.
- **terms-never-curated**: `resolve(model:template:)` MUST take
  `contextWindow`, `maxOutput`, `inputCostPerM`, and `outputCostPerM` only
  from the resolved `Offering`; the template's curated `ModelDetail` MUST NOT
  influence any of these four fields.
- **unknown-model-resolves-empty**: `resolve(model:template:)` MUST return a
  `ResolvedModel` (with `id` set to the requested name and `isEmpty == true`)
  for a name with no matching `Model` and no matching `Offering`, rather than
  returning `nil` or throwing.
- **shared-catalog-never-fails-to-exist**: `AIModelCatalog.shared` MUST always
  yield a usable, non-optional catalog: an empty catalog (`schemaVersion: 0`,
  no models, no offerings) whenever the bundled resource is missing,
  unreadable, or fails to decode.
- **load-is-total**: `load(from:)` MUST return `nil` — never throw — when the
  given bundle has no `model-catalog.json` resource, when the resource's
  `Data` cannot be read, or when the JSON fails to decode as
  `AIModelCatalog`.
- **index-built-at-init**: Every initializer MUST build `byName` and
  `byCanonicalID` synchronously from `models` during initialization, so
  `model(named:)` and `offering(templateID:model:)` are dictionary lookups
  and never re-scan `models`.
- **alias-collision-last-writer-wins**: When two `Model` entries in `models`
  share the same alias or the same `id` spelling as another entry's alias,
  `byName` MUST hold whichever model appears later in the `models` array for
  that spelling, because index population iterates `models` once, in array
  order, and later dictionary assignments overwrite earlier ones for the same
  key.
- **codable-round-trip-excludes-indexes**: Encoding and decoding an
  `AIModelCatalog` MUST round-trip only `schemaVersion`, `generatedAt`,
  `models`, and `offerings`; the private `byName`/`byCanonicalID` indexes MUST
  NOT appear on the wire and MUST be rebuilt from `models` on every decode.
- **value-type-is-freely-shared**: `Model`, `Offering`, `ResolvedModel`, and
  `AIModelCatalog` MUST be `Sendable` value types with no mutable state after
  initialization, so one loaded catalog instance MAY be read concurrently
  from any isolation domain without additional synchronization.
- **bundle-token-is-an-anchor-only**: `BundleToken` MUST exist solely so
  `Bundle(for: BundleToken.self)` resolves to this framework's own bundle
  regardless of which app embeds it; it carries no state and MUST NOT be used
  for anything but that one `Bundle(for:)` call.
- **schema-version-not-gated**: `AIModelCatalog.schemaVersion` MUST be decoded and stored but MUST NOT gate loading: no code reads or range-checks it, so any JSON-decodable payload loads whatever its `schemaVersion`, unlike the sibling `AIPluginDescriptor.schemaVersion`, which `AIPluginManager.swift` checks against `2...AIPluginDescriptor.currentSchemaVersion` and logs and skips when out of range. The catalog is a resource the framework ships itself, generated by `generate_model_catalog.py`, so a port that adds a range check is extending the contract, not restoring it.

## Appearance

Not applicable — this is a data lookup table (a `Sendable` value-type model),
not a visual component.

## States

Not applicable — this is a data lookup table (a `Sendable` value-type model),
not a visual component. The one runtime distinction this component has —
"resource loaded" versus "resource missing/unreadable/malformed, fell back to
empty" — is captured under Behavioral Requirements
(`shared-catalog-never-fails-to-exist`, `load-is-total`), not as a
visual-state table.

## Accessibility

Not applicable — this is a data lookup table (a `Sendable` value-type model),
not a visual component.

## Conformance Test Vectors

| ID | Requirements | Input | Expected |
|----|-------------|-------|----------|
| AMC-001 | model-identity | `model(named:)` called with `"gpt-oss-120b"`, `"openai/gpt-oss-120b"`, `"accounts/fireworks/models/gpt-oss-120b"`, `"OpenAI/GPT-OSS-120B"` | All four resolve to the `Model` whose `id == "gpt-oss-120b"` — `AIModelCatalogTests.lookupByEverySpelling` |
| AMC-002 | model-decoding-defaults | Decode `{"schemaVersion":1,"models":[{"id":"m"}]}` | `model(named: "m")`: `aliases == []`, `description == nil`, `capabilities == []`; `offerings.isEmpty == true` — `AIModelCatalogTests.decodesSparseEntries` |
| AMC-003 | offering-is-per-gateway-spelling | `offering(templateID: "groq", model: "openai/gpt-oss-120b")` vs. `offering(templateID: "groq", model: "accounts/fireworks/models/gpt-oss-120b")` | First returns `contextWindow == 131_072`; second returns `nil` (right model, wrong gateway's spelling) — `AIModelCatalogTests.offeringsAreScopedToTheGateway` |
| AMC-004 | offering-fields-independently-optional | Fireworks sample offering carries only `contextWindow: 1_000_000` | `offering(...).maxOutput`, `.inputCostPerM`, `.outputCostPerM` are all `nil`, not `0` — `AIModelCatalogTests.offeringsAreScopedToTheGateway` (sample fixture) |
| AMC-005 | lookup-resolution-order | `model(named: "OpenAI/GPT-OSS-120B")` (case fallback) and `model(named: "@cf/openai/gpt-oss-120b-fp8")` (no gateway spells it this way; canonical fallback) | Both resolve to `id == "gpt-oss-120b"` — `AIModelCatalogTests.lookupByEverySpelling`, `lookupFallsBackToCanonicalForm` |
| AMC-006 | canonicalization-strips-namespace | `canonicalID("meta-llama/Llama-3.3-70B-Instruct")` | `"llama-3.3-70b-instruct"` — `AIModelCatalogTests.canonicalID` |
| AMC-007 | canonicalization-strips-free-suffix | `canonicalID("deepseek/deepseek-r1:free")` | `"deepseek-r1"` — `AIModelCatalogTests.canonicalID` |
| AMC-008 | canonicalization-strips-quantization-suffix | `canonicalID("@cf/meta/llama-3.3-70b-instruct-fp8-fast")` | `"llama-3.3-70b-instruct"` (matches the longer `-fp8-fast`, not the shorter `-fp8`) — `AIModelCatalogTests.canonicalID` |
| AMC-009 | canonicalization-preserves-other-suffixes | `canonicalID("gpt-4-turbo")`, `canonicalID("grok-2-latest")` | Both unchanged — `AIModelCatalogTests.canonicalIDKeepsMeaningfulSuffixes` |
| AMC-010 | resolve-merges-three-sources | `resolve(model: "openai/gpt-oss-120b", template: groqTemplate)` | `description` from the shared `Model`, `contextWindow`/`inputCostPerM` from the groq `Offering` — `AIModelCatalogTests.resolveMergesSharedFactsWithGatewayTerms` |
| AMC-011 | curated-description-wins | Same call with `template.modelDetails = [ModelDetail(id: "openai/gpt-oss-120b", description: "Hand-written.")]` | `resolved.description == "Hand-written."`, not the shared blurb — `AIModelCatalogTests.curatedCopyWins` |
| AMC-012 | curated-tools-wins | (a) curated `tools: false` over shared capabilities containing `"tools"`; (b) no curated details, shared `capabilities == []` | (a) `resolved.tools == false`; (b) `resolved.tools == nil` (unknown, not false) — `AIModelCatalogTests.curatedCopyWins`, `toolsStayUnknownWithoutEvidence` |
| AMC-013 | terms-never-curated | Curated `ModelDetail` present (description/tools/goodFor only — it has no numeric fields) | `resolved.contextWindow` still equals the groq `Offering`'s `131_072`, unaffected by curation — `AIModelCatalogTests.curatedCopyWins` |
| AMC-014 | unknown-model-resolves-empty | `resolve(model: "nonesuch-9b", template: groqTemplate)` | `resolved.id == "nonesuch-9b"`, `resolved.isEmpty == true`, no throw — `AIModelCatalogTests.resolveUnknownModel` |
| AMC-015 | shared-catalog-never-fails-to-exist, load-is-total | `AIModelCatalog.load(from: Bundle(for: NSNull.self))` (no resource present) | Returns `nil`; `AIModelCatalog.shared.schemaVersion >= 0` still holds (fallback empty catalog, never a crash) — `AIModelCatalogTests.missingResourceIsSurvivable` |
| AMC-016 | load-is-total (positive path) | `AIModelCatalog.load()` against the framework's real bundle | Non-nil; `schemaVersion == 1`, `models.count > 100`, `offerings` non-empty, every model's `id` non-empty — `AIModelCatalogTests.shippedCatalogLoads` |
| AMC-017 | index-built-at-init | Construct `AIModelCatalog(schemaVersion: 1, generatedAt: nil, models: [Model(id: "gpt-oss-120b", aliases: [...])], offerings: [:])` via the memberwise initializer | `model(named:)` resolves every alias immediately with no separate build step — implicit in every test built on `AIModelCatalogTests.sample` |
| AMC-018 | alias-collision-last-writer-wins | `models: [Model(id: "a", aliases: ["shared"]), Model(id: "b", aliases: ["shared"])]` | `model(named: "shared")?.id == "b"` (the later array entry wins) — not exercised by a test in the given suite; traced directly to the index-build loop in `init(schemaVersion:generatedAt:models:offerings:)` |
| AMC-019 | codable-round-trip-excludes-indexes | `JSONEncoder().encode(sample)` then `JSONDecoder().decode(AIModelCatalog.self, from:)` | Re-decoded catalog's `model(named:)`/`offering(...)` behave identically to `sample`; the encoded JSON has only `schemaVersion`/`generatedAt`/`models`/`offerings` keys — not exercised by an explicit round-trip test in the given suite; traced to the `CodingKeys` enum and `encode(to:)` |
| AMC-020 | value-type-is-freely-shared | `AIModelCatalog.shared` read from multiple concurrent `Task`s in a `TaskGroup` | Every task observes the same, consistent lookups; the Swift compiler accepts the code without an actor hop, because `AIModelCatalog` is `Sendable` and immutable — not exercised by a dedicated concurrency test in the given suite; enforced statically by the `Sendable` conformance |
| AMC-021 | schema-version-not-gated | A hypothetical `model-catalog.json` with `"schemaVersion": 99` and an otherwise-valid shape | Decodes and loads successfully with no warning, unlike an out-of-range `AIPluginDescriptor.schemaVersion`, which `AIPluginManager` logs and skips — no test exists for this in the given suite, because the source implements no such check |

## Edge Cases

- **Empty lookup name.** `model(named: "")` — `byName` and `byCanonicalID`
  (via `canonicalID("") == ""`) both miss unless a `Model` literally has id
  `""`; the call MUST return `nil`.
- **Sparse decode.** A JSON model object carrying only `"id"` — `aliases` and
  `capabilities` MUST decode to `[]`, `description` MUST decode to `nil`,
  rather than failing the whole catalog's decode.
- **Model with zero aliases.** A `Model` whose `aliases == []` (e.g. the
  sample's `llama-3.3-70b-instruct`) MUST still resolve by its own `id`
  through `byCanonicalID`.
- **Empty catalog.** A catalog constructed with `models: []` MUST build empty
  indexes; every `model(named:)` call MUST return `nil` and every
  `resolve(...)` call MUST return an empty `ResolvedModel`, never throw.
- **Full-size shipped catalog.** The bundled resource carries 566+ models
  across 26+ gateways; the same dictionary-based lookup and merge logic MUST
  behave identically at that scale as in the two-model unit-test fixture — no
  size-dependent branch exists in the source.
- **Concurrent reads.** `Model`, `Offering`, `ResolvedModel`, and
  `AIModelCatalog` are `Sendable` value types built entirely inside their
  initializers, with no mutable stored state afterward, so concurrent reads
  from multiple threads or tasks MUST be safe with no locking — this is
  applicable (the catalog is read from AppKit UI code and could be read from
  background contexts too) and is safe by construction, not merely untested.
- **Missing, unreadable, or malformed bundle resource.** `load(from:)` MUST
  return `nil` for all three (`bundle.url(forResource:withExtension:)` returns
  `nil`; `Data(contentsOf:)` throws; `JSONDecoder().decode` throws), and
  `AIModelCatalog.shared` MUST fall back to the empty catalog rather than
  crash.
- **Id/alias collision between two distinct models.** No validation exists;
  the index-build loop's last write for a given key wins deterministically
  (see `alias-collision-last-writer-wins`), with no diagnostic distinguishing
  "intended override" from "generator bug." This is a stated fact, not a
  gap, because the resulting behavior is fully deterministic and traceable.
- **Curated `ModelDetail` lookup is exact-match only.** `template.modelDetail(for:)`
  (`AIPluginDescriptor.swift`) does `modelDetails?.first { $0.id == modelID }`
  — no lowercasing, no canonicalization fallback, unlike `model(named:)`.
  A template whose `modelDetails` entry is spelled differently from its
  `models` list entry SHOULD have matching spellings, or curation silently
  does not apply for that model.
- **Empty-string curated description is treated as absent.** `resolve(...)`
  MUST fall back to the shared `Model.description` when the curated
  `ModelDetail.description` is present but an empty string, exactly as it
  does when the curated value is `nil` — the two are indistinguishable to a
  caller of `resolve`.
- **Canonicalization suffix ordering.** A model name ending in `-fp8-fast`
  MUST have the whole four-token suffix stripped, not just `-fp8`, because
  the source checks the fixed suffix list in the order given and returns on
  the first match.
- **No network dependency (offline/disconnected: not applicable).** `AIModelCatalog`
  performs no network I/O of any kind — it reads only a bundled local JSON
  resource via `Bundle`/`Data(contentsOf:)`. There is no online/offline
  distinction for this component; connectivity loss elsewhere in the host app
  cannot affect it.

## Configuration

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| `bundle` | `Bundle` | `Bundle(for: BundleToken.self)` | Passed to `load(from:)`; the bundle to read `model-catalog.json` from. Tests override it (e.g. `Bundle(for: NSNull.self)`) to simulate a missing resource. |
| `model-catalog.json` | bundled resource | `packages/apple/AgenticToolkit/AIPluginKit/model-catalog.json` | Fixed resource filename, not configurable per call. Regenerated by `generate_model_catalog.py`; never hand-edited. |
| `schemaVersion` | `Int` | `0` for the empty fallback catalog; `1` in the shipped resource | Carried on the payload and on `AIModelCatalog()`'s private empty init; not validated at load time (see `schema-version-not-gated`). |
| `models`, `offerings` | `[Model]`, `[String: [String: Offering]]` | `[]`, `[:]` | Constructor parameters for building a catalog directly via `init(schemaVersion:generatedAt:models:offerings:)`, bypassing bundle loading entirely — the path the test suite's `sample` catalog and `AIModelCatalog()`'s empty fallback both use. |

## Deep Linking

Not applicable: `AIModelCatalog.swift` defines no routes, URLs, or navigable
destinations — it is an in-memory data table with no UI or navigation
surface of its own.

## Localization

Not applicable: `AIModelCatalog.swift` contains no user-facing string
literals of its own. The English-only `description`/`goodFor` prose it
carries is generated content data (from `generate_model_catalog.py` or a
curated `ModelDetail`), not localizable UI copy defined in this file — see
the hardcoded-strings fact under Compliance.

## Accessibility Options

Not applicable: `AIModelCatalog.swift` renders nothing and reads no
accessibility display setting (Reduce Motion, Increase Contrast,
Differentiate Without Color) — those apply only to the views that later
display a `ResolvedModel`, not to this data model.

## Feature Flags

Not applicable: `AIModelCatalog.swift` contains no feature-flag or
remote-config check of any kind; the catalog always loads (or falls back to
empty) unconditionally.

## Analytics

Not applicable: `AIModelCatalog.swift` emits no analytics events — it has no
telemetry call of any kind, success or failure.

## Privacy

Not applicable: `AIModelCatalog` carries no user data, credentials, or
tokens — its `Model`/`Offering` records are static, non-personal reference
data (model names, capabilities, pricing) shipped as a read-only bundled
resource and never written to or transmitted from the device by this type.

## Logging

Not applicable: `AIModelCatalog.swift` contains no `Logger`/`os_log`/`print`
call. Decode and load failures fail silently via `try?` and `guard let ...
else { return nil }`, with no diagnostic emitted — contrast
`AIPluginManager.swift`'s `logger.warning` for an incompatible
`AIPluginDescriptor`, which this file has no equivalent of (see
`schema-version-not-gated` in Behavioral Requirements).

## Platform Notes

- **SwiftUI**: The source (`packages/apple/AgenticToolkit/AIPluginKit/AIModelCatalog.swift`,
  bundled resource `model-catalog.json`, generator
  `packages/apple/AIPlugins/tools/generate_model_catalog.py`, tests
  `Tests/AIPluginKitTests/AIModelCatalogTests.swift`) has no SwiftUI
  dependency of its own — it is plain Swift, framework-agnostic. It is
  consumed today by AppKit view controllers under
  `macOS/Features/AIPlugins/Settings/` (`ModelPickerItem`,
  `ModelChooserViewController`, `ProviderPickerViewController`) purely as
  data; a SwiftUI consumer would call the same `resolve`/`model(named:)` API
  with no changes to this type.
- **Compose**: Model as Kotlin `data class`es — `data class Model(val id:
  String, val aliases: List<String> = emptyList(), val description:
  String? = null, val capabilities: List<String> = emptyList())` and a
  matching `Offering`/`ResolvedModel`, giving structural equality for free
  (matching Swift's `Equatable`). `kotlinx.serialization` with `@Serializable`
  and default values on the constructor reproduces `decodeIfPresent(...) ??
  []`. Replace `byName`/`byCanonicalID` with two `Map<String, Model>` built in
  an `init {}` block, and load `model-catalog.json` as an Android asset via
  `context.assets.open(...)` wrapped in a `runCatching { }.getOrNull()` to
  mirror `load(from:)`'s total (never-throwing) contract.
- **React/Web**: `interface Model { id: string; aliases: string[]; description?:
  string; capabilities: string[] }` plus matching `Offering`/`ResolvedModel`
  interfaces. Parse with a runtime validator (e.g. zod) instead of Swift's
  throwing `Codable` initializer, applying the same empty-array/`undefined`
  defaults explicitly since `JSON.parse` performs no schema coercion. Build
  `byName`/`byCanonicalID` as two `Map<string, Model>` at module load, and
  memoize the parsed catalog in a module-level `let shared: AIModelCatalog |
  null` (lazily computed on first access) as the equivalent of Swift's
  `static let shared`.
- **AppKit / UIKit**: No AppKit- or UIKit-specific API appears in this file;
  it is genuinely UI-framework-agnostic and only consumed by the AppKit view
  controllers named above. Nothing in this type needs to change to run under
  UIKit — only the consuming views differ.
- **WinUI 3**: This is the platform this recipe exists to steer. Model
  `Model`/`Offering`/`ResolvedModel` as C# `sealed record` types — e.g.
  `public sealed record Model(string Id, IReadOnlyList<string> Aliases,
  string? Description, IReadOnlyList<string> Capabilities)` — which give
  value semantics and structural equality matching Swift's `Equatable`.
  Decode with `System.Text.Json`'s `JsonSerializer.Deserialize<AIModelCatalog>`,
  using nullable properties with constructor defaults (or a custom
  `JsonConverter`) to reproduce `decodeIfPresent(...) ?? []`. Build
  `byName`/`byCanonicalID` as two `Dictionary<string, Model>` populated once
  in the record's constructor, exactly mirroring the Swift index-build loop
  (including last-writer-wins on a colliding key). Ship `model-catalog.json`
  as Content in the app package and load it via
  `Windows.ApplicationModel.Package.Current.InstalledLocation` (or an
  embedded-resource stream) instead of `Bundle.url(forResource:)`. Expose the
  loaded value through a `Lazy<AIModelCatalog>` (the `Lazy<T>` equivalent of
  Swift's `static let shared`), wrapping the file read and
  `JsonSerializer.Deserialize` call in a `try`/`catch` that falls back to an
  empty `AIModelCatalog` on any exception, matching `load(from:)`'s
  never-throw, empty-on-failure contract. `async`/`Task`-based file I/O is
  optional here since the resource is local and small; a synchronous read at
  startup is the direct port of the Swift `Bundle`/`Data(contentsOf:)` call.

## Reference Implementations

| Platform | Path |
|----------|------|
| apple | `packages/apple/AgenticToolkit/AIPluginKit/AIModelCatalog.swift` |

## Design Decisions

- **Decision**: Split shared, per-model facts (`Model`: description,
  capabilities) from per-offering, per-gateway terms (`Offering`: context
  window, output limit, prices), keyed by `templateId` and the gateway's own
  spelling of the model.
  **Rationale**: The same model (e.g. `gpt-oss-120b`) is served by many
  gateways; describing it inside every provider's descriptor would copy one
  blurb dozens of times and let the copies drift, while a model's context
  window and token prices genuinely differ per gateway (one number would be
  wrong nearly everywhere) — both facts are stated directly in the source's
  top-of-file documentation comment.
  **Approved**: pending
- **Decision**: `AIModelCatalog.shared` never fails to exist — a missing,
  unreadable, or malformed `model-catalog.json` yields an empty catalog
  rather than a crash or a thrown error surfacing to the caller.
  **Rationale**: per the source comment on `shared`, "a stale or absent
  catalog costs descriptions, and must never take the provider UI down with
  it" — losing model descriptions is an acceptable degradation; losing the
  entire provider picker is not.
  **Approved**: pending
- **Decision**: Curated `ModelDetail` overrides only the descriptive fields
  (`description`, `tools`, `goodFor`) in `resolve(...)`, never the numeric
  `Offering` terms.
  **Rationale**: per the source comment on `resolve`, curated fields are
  "hand-written for the few flagship models where the generated blurb isn't
  good enough, and a regeneration must never silently overwrite that
  judgement" — but a gateway's context window and prices are facts the
  gateway itself reports, which curated prose has no authority to change.
  **Approved**: pending
- **Decision**: `canonicalID(_:)` strips only a fixed, explicit set of
  quantization/precision suffixes (checked longest-first), and leaves every
  other trailing token — `-turbo`, `-latest`, `-fast`, `-thinking` — alone.
  **Rationale**: `generate_model_catalog.py`'s sibling `canonical_model_id`
  (which this Swift function must mirror per its own doc comment) states
  these suffixes are "the same weights served at lower precision" and safe to
  fold onto the base model, while `-turbo`/`-fast`/`-latest`/`-thinking`
  "name genuinely different models or aliases" and must never be folded.
  **Approved**: pending
- **Decision**: `AIModelCatalog.schemaVersion` is carried on the wire and
  stored, but — unlike `AIPluginDescriptor.schemaVersion` — is never gated
  against a known-compatible range before the catalog is used.
  **Rationale**: no comment in `AIModelCatalog.swift`,
  `generate_model_catalog.py`, or the shipped catalog explains this
  asymmetry; the recipe records the behavior as it is
  (`schema-version-not-gated`): the framework ships and regenerates its own
  catalog, so there is no foreign payload to guard against today.
  **Approved**: pending

## Compliance

| Check | Status | Category |
|-------|--------|----------|
| [graceful-degradation](agenticdevelopercookbook://compliance/reliability#graceful-degradation) | passed | Reliability |
| [explicit-error-handling](agenticdevelopercookbook://compliance/best-practices#explicit-error-handling) | partial | Best Practices |
| [data-integrity](agenticdevelopercookbook://compliance/reliability#data-integrity) | partial | Reliability |
| [no-hardcoded-strings](agenticdevelopercookbook://compliance/internationalization#no-hardcoded-strings) | failed | Internationalization |

Notes: graceful-degradation passes because `load(from:)`/`shared` always
resolve to a usable catalog (see `shared-catalog-never-fails-to-exist`).
explicit-error-handling is partial because every decode/load failure is
swallowed via `try?` with no diagnostic signal, unlike the sibling
`AIPluginManager` warning log for an incompatible plugin. data-integrity is
partial because `id`/alias collisions across `Model` entries silently
overwrite in the index with no validation (see
`alias-collision-last-writer-wins`). no-hardcoded-strings fails because
`Model.description` and curated `ModelDetail.goodFor` ship as English-only
prose with no localization mechanism (see Localization).

## Change History

| Version | Date | Author | Summary |
|---------|------|--------|---------|
| 1.0.0 | | | Initial creation |
| 1.0.1 | 2026-09-24 | Mike Fullerton | Phase 6 lint: re-audited open-question markers against the marker rules; kept markers are one-line named bullets. |
| 1.0.2 | 2026-09-25 | Mike Fullerton | Moved into the library cookbook; added Reference Implementations. |
