import Foundation

/// Marker so the Hub target has a compilable source before Task 11 fills it in.
public enum HubModule {
    /// Human-readable module version, bumped when the public API changes.
    public static let version = "0.1.0"
}
