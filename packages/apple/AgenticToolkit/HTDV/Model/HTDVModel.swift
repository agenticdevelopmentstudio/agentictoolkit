import Foundation

/// Marker so the HTDV target has a compilable source before Task 2 fills it in.
public enum HTDVModule {
    /// Human-readable module version, bumped when the public API changes.
    public static let version = "0.1.0"
}
