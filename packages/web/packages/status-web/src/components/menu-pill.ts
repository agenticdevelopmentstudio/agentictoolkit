// The compact bordered-mono-pill DropdownMenuTrigger look shared by the status
// dashboard's inline menus (the Stats span picker + the filter multi-selects).
// One home so a restyle (border, padding, popup-open highlight) lands once
// instead of drifting between the hand-authored triggers.
export const menuTriggerPillClass =
  "inline-flex cursor-pointer items-center gap-1.5 whitespace-nowrap rounded-md border border-apt-border bg-apt-bg px-2 py-1 font-mono text-[11px] normal-case text-apt-text-muted outline-none data-[popup-open]:text-apt-text";
